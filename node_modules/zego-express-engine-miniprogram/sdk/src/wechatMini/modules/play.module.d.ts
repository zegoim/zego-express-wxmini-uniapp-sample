import { ZegoExpressWebRTM } from "zego-express-engine-webrtm/sdk/src";
import { ZegoLogger, ZegoDataReport } from "../../common/zego.entity";
import { ZegoStreamCenterWechat } from "../../wechatMini/modules/zego.streamcenter.wechat";
import { StateCenter } from "../../common/stateCenter";
import { ENUM_DISPATCH_TYPE, ENUM_PLAY_SOURCE_TYPE } from "../../common/zego.entity";
export declare class PlayModule {
    private logger;
    private dataReport;
    private stateCenter;
    private streamCenter;
    private rtm;
    mixStreamList: {
        [index: string]: any;
    };
    preferPlaySourceType: ENUM_PLAY_SOURCE_TYPE;
    ultraPlaySourceType: string;
    constructor(logger: ZegoLogger, dataReport: ZegoDataReport, stateCenter: StateCenter, streamCenter: ZegoStreamCenterWechat, rtm: ZegoExpressWebRTM);
    startPlayingStream(streamID: string, playOption?: {
        streamParams?: string;
        isMix?: boolean;
        sourceType?: "CDN" | "BGP";
    }): Promise<{
        streamID: string;
        url: string;
    }>;
    stopPlayingStream(streamID: string): void;
    private startPlayingStreamFromCDN;
    private startPlayingStreamFromBGP;
    private doPlayStream;
    fetchPlayStreamUrl(streamID: string, urlType: string, sourceType: ENUM_DISPATCH_TYPE): void;
    private handleFetchStreamUrlRsp;
    setPreferPlaySourceType(sourceType: number): boolean;
}
