import { ZegoExpressWebRTM } from "zego-express-engine-webrtm/sdk/src";
import { ZegoDataReport, ZegoLogger } from "../../common/zego.entity";
import { ZegoStreamCenterWechat } from "../../wechatMini/modules/zego.streamcenter.wechat";
import { StateCenter } from "../../common/stateCenter";
import { ENUM_DISPATCH_TYPE, wxPublishOption } from "../../common/zego.entity";
export declare class PublishModule {
    private logger;
    private dataReport;
    private stateCenter;
    private streamCenter;
    private rtm;
    preferPublishSourceType: ENUM_DISPATCH_TYPE;
    customCdnUrl: string;
    ultraPlaySourceType: string;
    constructor(logger: ZegoLogger, dataReport: ZegoDataReport, stateCenter: StateCenter, streamCenter: ZegoStreamCenterWechat, rtm: ZegoExpressWebRTM);
    startPublishingStream(streamID: string, publishOption?: wxPublishOption): Promise<{
        streamID: string;
        url: string;
    }>;
    setPreferPublishSourceType(sourceType: 0 | 1 | 2, customUrl?: string): boolean;
    private fetchPublishStreamUrl;
    private handleFetchStreamPublishUrlRsp;
    private doPublishStream;
    stopPublishingStream(streamID: string): boolean;
}
