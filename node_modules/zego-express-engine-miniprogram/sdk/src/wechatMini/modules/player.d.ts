import { ZegoWechatMini } from "..";
import { ZegoWxPlayOption } from "../../../code/zh/ZegoExpressEntity.wechat";
import { PlayerAttributes, WxParams, ZegoLogger } from "../../common/zego.entity";
export declare class LivePlayer {
    private wxContext;
    private streamID;
    private zegoWechatMini;
    context: any;
    attributes: PlayerAttributes;
    isPlaying: boolean;
    logger: ZegoLogger;
    constructor(wxContext: any, streamID: string, zegoWechatMini: ZegoWechatMini, _wxContext?: any);
    setPlayerAttributes(config: any, callBack?: Function): void;
    play(playOption?: ZegoWxPlayOption): Promise<void>;
    stop(): void;
    pause(object: WxParams): void;
    resume(object: WxParams): void;
    mute(object: WxParams): void;
    requestFullScreen(object: {
        direction: number;
        success: Function;
        fail: Function;
        complete: Function;
    }): void;
    exitFullScreen(object: WxParams): void;
    requestPictureInPicture(object: WxParams): void;
    exitPictureInPicture(object: WxParams): void;
    snapshot(object: {
        quality: "raw" | "compressed";
        success: Function;
        fail: Function;
        complete: Function;
    }): void;
}
