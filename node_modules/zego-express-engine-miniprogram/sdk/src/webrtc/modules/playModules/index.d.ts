import { ZegoDataReport, ZegoLogger } from "../../../common/zego.entity";
import { StateCenter } from "../../../common/stateCenter";
import { webPlayOption, ZegoExpressWebRTM } from "../../../common/zego.entity";
import { ZegoStreamCenterWeb } from "../zego.streamCenter.web";
export declare class PlayModule {
    private logger;
    private dataReport;
    private stateCenter;
    private streamCenter;
    private rtm;
    constructor(logger: ZegoLogger, dataReport: ZegoDataReport, stateCenter: StateCenter, streamCenter: ZegoStreamCenterWeb, rtm: ZegoExpressWebRTM);
    startPlayingStream(streamID: string, playOption?: webPlayOption): Promise<MediaStream>;
    private _handlePlayStream;
    stopPlayingStream(streamID: string): void;
    mutePlayStream(streamID: string, type: string, mute: boolean): Promise<boolean>;
    setStreamAudioOutput(localVideo: any, audioOutput: string): boolean;
}
