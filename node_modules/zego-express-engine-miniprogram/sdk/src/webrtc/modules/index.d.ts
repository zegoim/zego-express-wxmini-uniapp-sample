import { ZegoDataReport, ZegoLogger } from "../../common/zego.entity";
import { StateCenter } from "../../common/stateCenter";
import { ZegoAudioContext, ZegoExpressWebRTM } from "../../common/zego.entity";
import { ZegoStreamCenterWeb } from "./zego.streamCenter.web";
import { PublishModule } from "./publishModules/index";
import { PlayModule } from "./playModules/index";
import { AudioMixModule } from "./externModules/audioModule";
import { AdvancedModule } from "./externModules/advancedModule";
export declare class RTCModules {
    private logger;
    private dataReport;
    private stateCenter;
    private streamCenter;
    private rtm;
    private ac;
    publishModule: PublishModule;
    playModule: PlayModule;
    audioMixModule: AudioMixModule;
    advancedModule: AdvancedModule;
    mediaEleSources: Array<{
        audio: HTMLMediaElement;
        node: MediaElementAudioSourceNode;
    }>;
    constructor(logger: ZegoLogger, dataReport: ZegoDataReport, stateCenter: StateCenter, streamCenter: ZegoStreamCenterWeb, rtm: ZegoExpressWebRTM, ac: ZegoAudioContext);
    init(): void;
    private bindWindowListener;
    private bindRTMListener;
    private bindStreamCenterHandler;
    private bindStreamHandler;
    private screenStreamFrom;
    private onPublishStateUpdateHandle;
    private WebrtcOnPublishStateUpdateHandle;
    bindListener(listener: string, callBack: Function): boolean;
    deleteListener(listener: string, callBack?: Function): boolean;
}
